# Tours Video Pipeline Prototype Notes

PROTOTYPE - fill this in after driving the terminal app.

## Verdict

Pending.

## Things To Watch

- Whether the `config.json` scene shape is enough for real `TourScene` creation.
- Whether generated `script.xml` should stay XML-like or become plain script plus delivery metadata.
- Whether avatar overlay needs a first-class local/manual import path before HeyGen API work.
- Whether FFmpeg composition-plan output is the right first durable test boundary for `TourRenderEngine`.
- Whether OpenRouter image-to-video should accept only public source image URLs in V1 or rely on `TourMediaStore` to create short-lived provider URLs from uploaded listing media.
